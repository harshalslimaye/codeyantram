API_URL=http://localhost:3001
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=

# Optional: web_fetch network policy (see README's "Network access" section). Both are
# off/empty by default - uncomment only if you need them.
# WEB_FETCH_ALLOW_PRIVATE=1
# WEB_FETCH_DENY_HOSTS=internal.example.com,another.internal

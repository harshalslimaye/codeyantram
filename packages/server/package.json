{
  "name": "@codeyantram/server",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun --env-file=../../.env test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.45",
    "ai": "^7.0.84",
    "hono": "^4.13.5",
    "@codeyantram/shared": "workspace:*"
  }
}

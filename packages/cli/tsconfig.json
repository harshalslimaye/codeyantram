{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // OpenTUI's React renderer supplies the JSX namespace, and its type
    // definitions reference DOM lib types.
    "lib": ["ESNext", "DOM"],
    "jsxImportSource": "@opentui/react"
  }
}
